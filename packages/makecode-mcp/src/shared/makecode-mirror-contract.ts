// Shared by the server that mirrors MakeCode's pages and the view that turns
// them into blobs, so neither target has to import the other's module.
export const SIM_URL_PLACEHOLDER = "__MKCP_SIM_BLOB__";
export const WORKER_URL_PLACEHOLDER = "__MKCP_WORKER_BLOB__";
